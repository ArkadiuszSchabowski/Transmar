import { AssemblyLineWorkstationRepository } from './assembly-line-workstation-repository';

describe('AssemblyLineWorkstationRepository', () => {
  it('should be defined', () => {
    expect(new AssemblyLineWorkstationRepository()).toBeDefined();
  });
});
