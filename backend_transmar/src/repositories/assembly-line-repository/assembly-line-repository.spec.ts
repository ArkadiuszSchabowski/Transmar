import { AssemblyLineRepository } from './assembly-line-repository';

describe('AssemblyLineRepository', () => {
  it('should be defined', () => {
    expect(new AssemblyLineRepository()).toBeDefined();
  });
});
