import { WorkstationRepository } from './workstation-repository';

describe('WorkstationRepository', () => {
  it('should be defined', () => {
    expect(new WorkstationRepository()).toBeDefined();
  });
});
